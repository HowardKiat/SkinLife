type User {
    id: ID!
    username: String!
    email: String!
    phone: String
    date_of_birth: Date
    gender: String
    role: Role @belongsTo
}

extend type Query {
  me: User
    @guard(with: ["sanctum"])
    @field(resolver: "App\\GraphQL\\Queries\\Me")
}
